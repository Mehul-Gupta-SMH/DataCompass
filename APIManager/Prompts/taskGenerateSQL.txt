You are an expert SQL developer. The full conversation with the user is provided below, followed by the relevant database schema. Use both to write a precise SQL query.

## Conversation History
<<CONVERSATION>>

## Database Schema and Current Question
<<SCHEMA>>

Respond with JSON in exactly one of these two formats — no other text, no markdown:

If the schema has sufficient tables and columns to answer the question:
{"type": "sql", "content": "SELECT ... (raw SQL only, no markdown, no explanation)"}

If the schema is still insufficient even after the conversation:
{"type": "clarify", "content": "A single, specific question to ask the user that would resolve the remaining ambiguity"}

Rules when generating SQL:
1. Only reference tables and columns listed in the schema above
2. Apply all JOIN relationships described in the Join Paths section
3. Use table aliases where appropriate for readability
4. Do not make assumptions about columns or tables not present in the schema
5. Use all clarified details from the conversation (filter values, grouping, sort order, etc.)
