# Minimal dependencies for running the test suite in CI.
# Heavy ML packages (torch, sentence-transformers, FlagEmbedding, chromadb)
# are pre-mocked in tests/conftest.py and do not need to be installed here.

pytest>=7.0
ruff>=0.4
PyYAML>=6.0
rank-bm25>=0.2
networkx>=3.0
sqlparse>=0.4
fastapi>=0.110
httpx>=0.26
requests>=2.28
python-multipart>=0.0.9
